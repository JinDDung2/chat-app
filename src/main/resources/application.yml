server:
  port: 8081

spring:
  thymeleaf:
    cache: false
    prefix: file:src/main/resources/templates/
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

logging:
  level:
    com.likelion.chatapp: debug
